#pragma once

namespace opt
{
    enum opt
    {
        flag_set = 0,
        input_data_type = 1000,
        input,
        output_data_type,
        output,
        algorithm,
        key_data_type,
        key,
        iv_data_type,
        iv,
        block_cipher_mode,
        padding,
        help
    };
}
